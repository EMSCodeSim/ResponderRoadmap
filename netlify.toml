[build]
  command = "npm test && npm run db:push && npm run db:demo:push && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
